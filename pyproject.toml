[tool.poetry]
name = "sat-chat"
version = "1.0.0"
description = "SatChat Marine Debris Monitoring"
authors = ["Telefix Team"]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "*"
uvicorn = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"