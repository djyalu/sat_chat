[tool.poetry]
name = "app"
version = "1.0.0"
description = ""
authors = [""]

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.scripts]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"