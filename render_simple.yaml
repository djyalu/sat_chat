services:
  - type: web
    name: sat-chat-api
    runtime: python
    buildCommand: pip install fastapi uvicorn
    startCommand: uvicorn simple_app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"