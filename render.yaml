services:
  - type: web
    name: sat-chat-api
    env: python
    buildCommand: poetry install
    startCommand: poetry run uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"